{
  "name": "loyalty-os",
  "version": "0.1.0",
  "private": true,
  "description": "Druto-like Loyalty OS — QR-first digital loyalty platform for local businesses (multi-tenant B2B SaaS).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev -w @loyalty/api",
    "dev:web": "npm run dev -w @loyalty/web",
    "build": "npm run build -w @loyalty/api && npm run build -w @loyalty/web",
    "db:setup": "npm run db:push -w @loyalty/api && npm run seed -w @loyalty/api",
    "seed": "npm run seed -w @loyalty/api",
    "test": "npm run test -w @loyalty/api",
    "lint": "npm run lint -w @loyalty/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
